// Note: Export a firebase.config.js file with your firebase details.
import firebaseCfg from './firebase.config';

// Settings configured here will be merged into the final config object.
export default {
  firebaseCfg
}
